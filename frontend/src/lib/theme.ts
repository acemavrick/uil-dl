// single source of truth for accent colors.
// change the base hex values here to re-skin the entire app.
export const theme = {
	accents: {
		primary: { name: 'vermillion', base: '#E06B56' },
		secondary: { name: 'gold', base: '#D4A04A' },
		tertiary: { name: 'slate', base: '#6B8DB0' },
	},
} as const;
